extends layout

block content
  .hero
    .hero-titles
      h1 The King's Cup Rule Master
      h2 Draw a king? Need a rule?
    .hero-cta
      a.pure-button.pure-button-primary.pure-button-large(href='/rule') Get a rule
  .content
    p
      span.lead Born out of frustration 
      | on those late nights 
