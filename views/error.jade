extends layout

block content
  .header
    h1= message
    h2= status
  .content
    p.error-message.text-center We forgive you, but we hope it doesn't happen again.
    if stack
      pre #{stack}
    
     